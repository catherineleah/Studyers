<%= form_for([@user,@notebook]) do |f| %>
  <% if @notebook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notebook.errors.count, "error") %> prohibited this notebook from being saved:</h2>

      <ul>
      <% @notebook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name "Name (or course name)" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
     <%= f.label :course, "Course code / ID" %><br />
     <%= f.text_field :course %>
  </div>

  <div class="field">
     <%= f.label :lecturer %><br />
     <%= f.text_field :lecturer %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
