<div class="banner profile">
  <div class="container">
    <h2><%=t :profile %></h2>
  </div>
</div>
<div id="profile" class="container">
  <p id="notice"><%= notice %></p>
  <%if role?(@user) == 'teacher' %>
    <% if current_user?(@user) %>
    <div>
      Dear <%= @user.name%>,<br />
      We are working real hard on creating your space in Studyers. </br>
      Soon you would be able to create courses & lessons<br />
      and to upload your lesson resources into them. <br />
      The lesson resources will include your presentations, links, videos and images. <br />
      We are about to finish the development of the teacher's space in Studyers real soon,<br />
      We will let you know when we're done.
      <% 
        # The teacher render is currently for dev only, as it is not finished
      %>
      <% if Rails.env != "development" >
        <%= render 'teacher' %>
      <% end %>
    </div>
    <% else %>
      <div><%= @user.name %> teacher's space coming soon.</div>
    <% end%>
    
  <% else %>
    <%# render 'layouts/profile' %>
    <% if current_user?(@user) %>      
      <%= render 'my' %>
    <% else %>
      <%= render 'user' %>
    <% end %>
  <% end %>
</div>