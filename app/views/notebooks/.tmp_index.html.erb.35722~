<h1>Listing notebooks</h1>

<table>
  <tr>
    <th>Name</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @notebooks.each do |notebook| %>
  <tr>
    <td><%= notebook.name %></td>
    <td><%= notebook.user_id %></td>
    <td><%= link_to 'Show', user_notebook_path(@user, notebook) %></td>
    <td><%= link_to 'Edit', edit_user_notebook_path(@user, notebook) %></td>
    <td><%= link_to 'Destroy', [@user, notebook], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Notebook', new_user_notebook_path %>
