<div class="row">
  <h1 class="notebook-name span12"><%= @notebook.name %></h1>
  <% if @current_user.id == @notebook.user_id %>
    <%= link_to '+ Create a new Lesson', new_notebook_lesson_path, :class => "btn info" %>
  <% end %>
  <h2 class="lesson-list">list of lessons</h2>
</div>
<div class="row">
  <% if @lessons.count > 0 %>
    <% @lessons.each do |lesson| %>
      <div class="lesson">  
        <h3 class="lesson-title"><%= link_to lesson.title, notebook_lesson_path(@notebook, lesson) %></h3>
        <div class="lesson-date"><span>Created at:</span> <%= lesson.created_at %></div>
        <% if can? :update, lesson %>
          <div class="edit-lesson"><%= link_to 'Edit this lesson', edit_notebook_lesson_path(@notebook, lesson), :class =>"btn", :title => "Edit lesson"  %></div>
          <div class="delete-lesson"><%= link_to '- Delete this lesson', [@notebook, lesson], :class =>"btn", :confirm => 'Are you sure?', :method => :delete, :title => "Delete lesson" %></div>
        <% end %>
      </div>
    <% end %>
  <% else %>
    <% if @current_user.id == @notebook.user_id %>
      <div class="empty-lesson">Add a <%= link_to 'first lesson', new_notebook_lesson_path %></div>
    <% end %>
  <% end %>
</div>

